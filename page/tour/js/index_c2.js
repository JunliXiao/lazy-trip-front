// =========================點擊確認在右方渲染============================== //
$("button.button.comfirm_btn").on("click", function () {
  let trip_date = $("input.trip_date").value;
  let attraction_search = $("input.attraction_search").value;
  let attraction_time = $("input.attraction_time").value;
  let attraction_stay_hour = $("input.attraction_stay_hour").value;
  let attraction_stay_min = $("input.attraction_stay_min").value;
  let trip_end_date = $("input.trip_end_date").value;
});
